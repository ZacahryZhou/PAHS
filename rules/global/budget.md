# Budget rules loaded with every run.
# 每次 Run 都会加载的预算规则。

- Check token and cost budgets before expensive steps.
- Prefer cheaper models when alert thresholds are reached.
- Pause and ask the user before exceeding hard limits.
